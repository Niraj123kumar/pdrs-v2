'use strict';

const { Router } = require('express');

/**
 * AI routes (question generation, answer evaluation) backed by groq-sdk.
 * Route handlers are implemented in a later phase.
 */
const router = Router();

module.exports = router;
