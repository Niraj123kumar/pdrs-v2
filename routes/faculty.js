'use strict';

const { Router } = require('express');

/**
 * Faculty-facing routes (view assigned students, manage session requests).
 * Route handlers are implemented in a later phase.
 */
const router = Router();

module.exports = router;
