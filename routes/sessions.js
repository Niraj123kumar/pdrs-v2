'use strict';

const { Router } = require('express');

/**
 * Rehearsal session routes (create, fetch, submit answers, complete).
 * Route handlers are implemented in a later phase.
 */
const router = Router();

module.exports = router;
