'use strict';

const { Router } = require('express');

/**
 * Live panel session routes (start, join, annotate, end).
 * Route handlers are implemented in a later phase.
 */
const router = Router();

module.exports = router;
